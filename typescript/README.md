# ElusiveBot Schema - Typescript

## Installation

From the root repository directory:

* ./gradlew build

From the *pkg* directory:

* npm install
    * Only needs to be performed on a clean checkout and dependencies change.
* npm run build
* npm link
